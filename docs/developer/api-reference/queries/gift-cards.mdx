---
id: gift-cards
title: giftCards
hide_table_of_contents: false
---

List of gift cards.

Requires one of the following permissions: MANAGE_GIFT_CARD.

```graphql
giftCards(
  sortBy: GiftCardSortingInput
  filter: GiftCardFilterInput
  search: String
  before: String
  after: String
  first: Int
  last: Int
): GiftCardCountableConnection
```

### Arguments

#### [`sortBy`](#) ([`GiftCardSortingInput`](../../../developer/api-reference/inputs/gift-card-sorting-input))

Sort gift cards.

#### [`filter`](#) ([`GiftCardFilterInput`](../../../developer/api-reference/inputs/gift-card-filter-input))

Filtering options for gift cards.

#### [`search`](#) ([`String`](../../../developer/api-reference/scalars/string))

Search gift cards by email and name of user, who created or used the gift card, and by code.

#### [`before`](#) ([`String`](../../../developer/api-reference/scalars/string))

Return the elements in the list that come before the specified cursor.

#### [`after`](#) ([`String`](../../../developer/api-reference/scalars/string))

Return the elements in the list that come after the specified cursor.

#### [`first`](#) ([`Int`](../../../developer/api-reference/scalars/int))

Retrieve the first n elements from the list. Note that the system only allows fetching a maximum of 100 objects in a single query.

#### [`last`](#) ([`Int`](../../../developer/api-reference/scalars/int))

Retrieve the last n elements from the list. Note that the system only allows fetching a maximum of 100 objects in a single query.

### Type

#### [`GiftCardCountableConnection`](../../../developer/api-reference/objects/gift-card-countable-connection)
